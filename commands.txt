python train_kpop_singers.py `
  --root "K:\Python Projects\Voice Recognition" `
  --group "IVE" `
  --sr_in 22050 `
  --sr_ecapa 16000 `
  --chunk-sec 2.0 `
  --epochs 20 `
  --batch-size 32 `
  --lr 5e-4 `
  --val-split 0.15 `
  --num-workers 8 `
  --save-dir "K:\Python Projects\Voice Recognition\models"

python train_kpop_singers.py `
  --root "K:\Python Projects\Voice Recognition" `
  --group "BTS" `
  --sr_in 22050 `
  --sr_ecapa 16000 `
  --chunk-sec 2.0 `
  --epochs 10 `
  --batch-size 32 `
  --lr 5e-4 `
  --val-split 0.15 `
  --num-workers 8 `
  --save-dir "K:\Python Projects\Voice Recognition\models"

- What did I use for AI
- How I made the project
- What kind of AI to implement my stuff
- Explain what I want
- Show leadership abilities

- Fix members moving around with short lines (order gets messed up so we need debugging)
- Remove stupid stutter for double "jump" if there is music video
- Important: VERY BAD AT OVERLAP: EG. yujin sings before Wonyoung ends and model forgets Wonyoung completely
- Remove duplicate labels double time if the same member label overlaps
- Fix the issue with the window being squashed on different monitors (shouldn't be static)
- Fix members moving up and down with variable window heights (works normally but struggles when the window size is changed because distance to 'jump' is altered)
- Have specific adlib head because model can't predict adlibs but great at long solo parts
- Must figure out how to detect lines under 1 second long because "Fake love" is short and model ignores it. Chorus is almost perfect

✅ MODEL / HEADS

Add a harmony head to detect harmonized vocals distinctly.

Add an ad-lib head to capture short, irregular vocal bursts.

Fuse multi-window embeddings (40ms + 200ms + 500ms) in the transformer.

✅ TEMPORAL / CONTEXT

Add a 400–600ms context window to stabilize identity for short/choppy lines.

Apply temporal smoothing (median filter / hysteresis) during inference.

Enforce a minimum-duration rule to clean up micro false-negatives.

✅ TRAINING DATA

Gather more short-line (<1s) phrase samples per member.

Add harmony-specific examples (upper + lower harmonies).

Add more real group-song line samples across multiple eras.

Ensure each member has low/mid/high register examples.

Create a dedicated dataset of ad-libs and one-syllable vocals.

✅ AUGMENTATION

Add pitch shift (±1–2 semitones) for register robustness.

Add formant shift to simulate timbre variation.

Add tempo shift to match different song BPM textures.

Add mild reverb/EQ/noise to simulate real mixing conditions.

✅ LABELING

Label harmony regions explicitly in JSON.

Label ad-lib segments separately from main vocals.

Expand labels to differentiate lead vs background clearer.

✅ ARCHITECTURE TUNING

Use class-weighted loss to boost recall for weak/quiet members.

Use focal loss to improve classification for harmonies/ad-libs.

Increase dropout in high-confusion contexts (heavy harmonies).